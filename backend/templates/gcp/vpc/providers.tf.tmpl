terraform {
  backend "gcs" {}
}

provider "google" {
  project = "{{ .ProjectID }}"
  region  = "{{ .Region }}"
}

provider "google-beta" {
  project = "{{ .ProjectID }}"
  region  = "{{ .Region }}"
}
